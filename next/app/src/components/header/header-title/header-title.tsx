import styles from '../../../../styles/components/header/header-title/header-title.module.scss';

const HeaderTitle = () => {
    return (
        <h1 className={styles.headerTitle}>
            DuntriXIV
        </h1>
    )
}

export default HeaderTitle;