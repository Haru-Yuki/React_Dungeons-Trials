import {Typography} from "@mui/material";

const FooterTitle = () => {
    return (
        <Typography variant="h2" sx={{
            color: 'text.primary',
            marginTop: '-15px'
        }}>
            DuntriXIV
        </Typography>
    )
};

export default FooterTitle;